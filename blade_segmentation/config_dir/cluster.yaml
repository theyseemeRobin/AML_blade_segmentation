defaults:
  - cluster_default

# Training parameters
clustering_algorithm: "kmeans"
num_clusters: 2
n_iter: 2

resume_path: "models/2block-finetune.pth"

# Model paths
basepath: data/optic_thermal/Images/trainval
output_path: results

attn_drop_t: 0.4
path_drop: 0.2
num_t: 2

# System settings
wandb_online: true